apiVersion: v1
kind: Service
metadata:
  name: {{ include "nixery.fullname" . }}
  labels:
    {{- include "nixery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nixery.selectorLabels" . | nindent 4 }}